@require: base/block
@require: base/inline

module ReviewComment : sig

  val write : string -> inline-text -> context -> inline-boxes
  val render : string -> inline-text -> context -> block-boxes

end = struct

  let write reviewer comment ctx = Inline.nil
  let render reviewer comment ctx = Block.nil

end
